# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Toolbox\OpenApiServerReader:
        arguments:
            $httpClient: '@http_client'

    # The MCP server role needs the full Symfony container so tool handler
    # classes resolve from real DI (repositories, entity manager, logging).
    App\Mcp\Server\TaskServerFactory:
        public: true
        arguments:
            $container: '@service_container'

    # Referenced by name from the MCP tool handler resolution (and the
    # gating tests, which must exercise the real persisted gate).
    App\Mcp\Server\TaskCrud:
        public: true

    App\Mcp\Server\TaskTools:
        public: true

    # Protocol → reader map for the catalog synchronizer (SPEC §7).
    App\Toolbox\ServerReaderRegistry:
        arguments:
            $readers:
                mcp: '@App\Toolbox\McpServerReader'
                openapi: '@App\Toolbox\OpenApiServerReader'

    # Run engine (SPEC §3, §5): loop, prompt compiler, budgets.
    App\Llm\LlmClient:
        arguments:
            $baseUrl: '%env(TASKLOOM_LLM_BASE_URL)%'
            $model: '%env(TASKLOOM_LLM_MODEL)%'
            $apiKey: '%env(TASKLOOM_LLM_API_KEY)%'
            $timeoutSeconds: '%env(int:TASKLOOM_LLM_TIMEOUT)%'

    # HTTP Basic admin auth (SPEC §4.3): one env-provided password, one
    # admin principal, constant-time comparison. Fail closed when unset.
    App\Security\AdminAuthenticator:
        arguments:
            $adminPassword: '%env(TASKLOOM_ADMIN_PASSWORD)%'

    App\Context\ContextWindow:
        arguments:
            $contextLimitTokens: '%env(int:TASKLOOM_CONTEXT_LIMIT)%'
            $maxToolOutputPct: '%env(float:TASKLOOM_MAX_TOOL_OUTPUT_PCT)%'
            $windowTailExchanges: '%env(int:TASKLOOM_WINDOW_TAIL_EXCHANGES)%'

    App\RunEngine\RunEngine:
        arguments:
            $budgets:
                step_budget: '%env(int:TASKLOOM_STEP_BUDGET)%'
                tool_retries: '%env(int:TASKLOOM_TOOL_RETRIES)%'
                circuit_breaker: '%env(int:TASKLOOM_CIRCUIT_BREAKER)%'
